// src/tests/setup.ts — Frontend test setup
import "@testing-library/jest-dom";
// Adds custom matchers like:
// expect(element).toBeInTheDocument()
// expect(element).toHaveClass("btn-primary")
// expect(element).toBeVisible()
