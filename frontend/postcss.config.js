export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // autoprefixer: automatically adds vendor prefixes to CSS
    // e.g. -webkit-backdrop-filter for Safari
    // You never need to write vendor prefixes manually
  },
};
